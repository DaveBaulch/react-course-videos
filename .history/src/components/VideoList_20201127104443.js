import React from 'react';
import VideoItem from './VideoItem';

const VideoList = ({videos}) => {
  console.log(videos);

  cons VideoList = ({ videos }) => {
    videos.map((video) => {
      retur
  });

  return (
    <div>
    {videos.length}
    <VideoItem />
    </div>

  )
};

export default VideoList;
